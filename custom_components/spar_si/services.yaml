search_products:
  fields:
    query:
      required: true
      example: "mleko"
      selector:
        text:
    max_results:
      required: false
      default: 10
      example: 5
      selector:
        number:
          min: 1
          max: 50
          mode: box
